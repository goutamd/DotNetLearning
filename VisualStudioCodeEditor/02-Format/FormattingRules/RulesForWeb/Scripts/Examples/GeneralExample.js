// Use Tools/Options/Text Editor/JavaScript/Formatting 

function Add(x, y, z) {
  // choose when the format happens
  // 1. Press Enter
  // 2. Press Semicolon         -- ;
  // 3. Press close curly brace -- } 
  // 4. On Paste command

  if (x > 10) {
    // do something with x
    // do something with x
    do {

    } while (      true);
  }

  if (y > 200) {
    // do something with y
    do {

    } while (true);
  
  }


}
